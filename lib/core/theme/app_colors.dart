import 'package:flutter/material.dart';

class NaviColors {
  NaviColors._();

  // Brand
  static const primary = Color(0xFF3D3580); // deep navy-purple
  static const primaryLight = Color(0xFF7E6DB0); // medium purple
  static const primaryPale = Color(0xFFE0DCF5); // lavender tint

  // Background
  static const background = Color(0xFFF5F3F0); // soft off-white
  static const surface = Color(0xFFFFFFFF);

  // Accent sparkle colors (match the prototype)
  static const sparkPink = Color(0xFFEF9A9A);
  static const sparkBlue = Color(0xFF81D4FA);
  static const sparkYellow = Color(0xFFFFD54F);
  static const sparkGreen = Color(0xFFA5D6A7);
  static const sparkPurple = Color(0xFFB39DDB);
  static const sparkTeal = Color(0xFF80CBC4);

  // Career match badge colors
  static const matchHigh = Color(0xFF4CAF50); // green — high demand
  static const matchMed = Color(0xFFFFA726); // amber — medium
  static const matchLow = Color(0xFFEF5350); // red — low

  // Text
  static const textDark = Color(0xFF2C2560);
  static const textMid = Color(0xFF7E6DB0);
  static const textLight = Color(0xFF9E95C7);
  static const textMuted = Color(0xFFB0AAC8);
}
